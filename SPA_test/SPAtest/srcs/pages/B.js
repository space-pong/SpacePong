import BaseComponent from '../BaseComponent.js';

export default class B extends BaseComponent {
    template() {
        return `<div>B Page</div>`;
    }

    setEvent() {
        // Add any event listeners for B page if needed
    }
}