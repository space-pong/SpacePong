import BaseComponent from '../BaseComponent.js';

export default class A extends BaseComponent {
    template() {
        return `<div>A Page</div>`;
    }

    setEvent() {
        // Add any event listeners for A page if needed
    }
}